% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/write_group_to_pyproject.R
\name{read_dep_groups}
\alias{read_dep_groups}
\title{Parse \verb{[dependency-groups]} from a TOML file's lines}
\usage{
read_dep_groups(lines)
}
\arguments{
\item{lines}{Character vector of file lines.}
}
\description{
Returns a list with \code{header_idx} (line index of \verb{[dependency-groups]}),
\code{block_end} (line index of last line in the block, exclusive of the
next section header), and \code{entries} (named list mapping group name to
character vector of dependency strings). Returns \code{NULL} if the table
is not present.
}
\details{
Only handles the canonical \verb{[dependency-groups]} table form with
arrays of strings — both inline (\verb{name = ["a", "b"]}) and multi-line
(\verb{name = [\\n  "a",\\n  "b",\\n]}). Other TOML constructs in the file
are preserved verbatim by the splice helpers; this parser only
inspects the dep-groups block.
}
\keyword{internal}
