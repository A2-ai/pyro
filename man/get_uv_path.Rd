% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/uv_path.R
\name{get_uv_path}
\alias{get_uv_path}
\title{Locate the uv binary}
\usage{
get_uv_path(quiet = FALSE)
}
\arguments{
\item{quiet}{If \code{TRUE}, suppress the log message when uv is not found.}
}
\value{
Path to the uv executable, or \code{NULL} if none is found.
}
\description{
Searches \code{PATH} first, then known install locations
(\verb{~/.local/bin/uv}, \verb{~/.cargo/bin/uv}) cross-platform. Respects the
\code{HOME} environment variable for test isolation.
}
\details{
Use this to detect whether uv is installed without triggering an
install. \code{\link[=initialize_python]{initialize_python()}} performs the install; this helper is
a pure lookup.
}
\examples{
\dontrun{
uv_path <- get_uv_path()
if (is.null(uv_path)) message("uv not installed yet")
}
}
