% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/initialize_python.R
\name{initialize_python}
\alias{initialize_python}
\title{Initialize the uv-managed Python virtual environment}
\usage{
initialize_python(
  continue = NULL,
  venv_dir = get_proj_dir(),
  uv_version = getOption("uv.version"),
  groups = NULL,
  pyproject_dir = NULL
)
}
\arguments{
\item{continue}{Optional. \code{"Y"} / \code{"n"} or \code{TRUE} / \code{FALSE} to
bypass the interactive prompt. The same prompt covers both the uv
install and the seed step (when the project has no \code{pyproject.toml}
yet).}

\item{venv_dir}{Parent directory of \verb{.venv/}. Defaults to
\code{\link[=get_proj_dir]{get_proj_dir()}}.}

\item{uv_version}{Version of uv to install. Defaults to
\code{getOption("uv.version")}. When \code{NULL} (option unset), behavior
is: if uv is already installed, its existing version is used (no
reinstall); otherwise \code{"0.7.8"} is installed as a conservative
fallback.}

\item{groups}{Character vector of dependency-group names from the
project's \code{pyproject.toml} (e.g. \code{"reportifyr"}, \code{"presentifyr"}).
When \code{NULL} (default), every declared group is installed via
\verb{uv sync --frozen --all-groups}. When supplied, uv runs in
additive mode (\verb{uv sync --frozen --inexact --group <g> ...}) so
sibling fyr-package calls coexist without removing each other's
deps. Group names are validated by uv; an unknown group surfaces
uv's error.}

\item{pyproject_dir}{Directory containing the project's
\code{pyproject.toml}. Defaults to \code{venv_dir}. Override for non-default
project layouts (LLM agents, monorepos, closed-source projects
with secret toml elsewhere). The directory must already exist;
pyro will not create it.}
}
\value{
Invisibly \code{NULL}.
}
\description{
Ensures uv is installed at \code{uv_version}, then locks and syncs the
project's \verb{.venv/} against \verb{<pyproject_dir>/pyproject.toml}. If no
\code{pyproject.toml} exists at \code{pyproject_dir}, one is seeded from
pyro's bundled spec (templated with the project's directory
name) before lock and sync.
}
\details{
The project's \code{pyproject.toml} is the source of truth. pyro
seeds it on first call but never modifies an existing toml — group
upserts are the responsibility of sibling-package wrappers via
\code{\link[=write_group_to_pyproject]{write_group_to_pyproject()}}.
}
\examples{
\dontrun{
initialize_python()                              # all groups
initialize_python(groups = "reportifyr")        # only reportifyr's group
initialize_python(groups = "presentifyr")       # only presentifyr's group
initialize_python(pyproject_dir = "~/proj")     # custom project root
}
}
