% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/run_python_script.R
\name{run_python_script}
\alias{run_python_script}
\title{Run a Python script inside the uv-managed venv}
\usage{
run_python_script(
  uv_path,
  args,
  venv_path,
  script_name,
  pythonpath = NULL,
  stderr_callback = NULL,
  verbose_env = NULL
)
}
\arguments{
\item{uv_path}{Path to the uv executable.}

\item{args}{Arguments to pass to uv (e.g., \code{c("run", "-m", "my_cli")}).}

\item{venv_path}{Path to the \verb{.venv/} directory (sets \code{VIRTUAL_ENV}).}

\item{script_name}{Human-readable label used in the error message.}

\item{pythonpath}{Optional directory to expose as \code{PYTHONPATH} (for
host packages whose Python modules live under their \verb{inst/python/}).}

\item{stderr_callback}{Optional \verb{function(line, proc)} callback invoked
once per complete line of stderr, with the line terminator stripped.
If \code{NULL}, stderr is streamed to the console via \code{cat()}. Lines are
reassembled before dispatch, so a callback never sees a partial line
(see Details).}

\item{verbose_env}{Optional name of an environment variable to surface
to the subprocess (for example, the caller's verbosity control).}
}
\value{
The result from \code{\link[processx:run]{processx::run()}}.
}
\description{
Shells out to uv via \code{\link[processx:run]{processx::run()}} with the venv's \code{VIRTUAL_ENV}
and (optionally) a caller-supplied \code{PYTHONPATH}. pyro does not
persist subprocess output to any file — routing of the Python
subprocess' stderr is entirely the caller's concern. Supply
\code{stderr_callback} to capture, reformat, or filter stderr lines; wrap
this call in \code{tryCatch()} if you want to act on a subprocess crash.
}
\examples{
\dontrun{
paths <- pyro::get_venv_uv_paths()
pyro::run_python_script(
  uv_path     = paths$uv,
  venv_path   = paths$venv,
  args        = c("run", "-m", "my_module"),
  script_name = "my_module"
)
}

}
