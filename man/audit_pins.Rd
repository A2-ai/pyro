% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/audit_pins.R
\name{audit_pins}
\alias{audit_pins}
\title{Report drift between project pyproject pins and pyro's bundled spec}
\usage{
audit_pins(pyproject_dir = get_proj_dir(), groups = NULL)
}
\arguments{
\item{pyproject_dir}{Directory containing the project's
\code{pyproject.toml}. Defaults to \code{\link[=get_proj_dir]{get_proj_dir()}}.}

\item{groups}{Optional character vector of group names to scope the
audit to. When \code{NULL} (default), every bundled-known group present
in the project toml is audited. When supplied, drift in groups
outside this set is silenced — a sibling group's drift will
surface the next init that targets it, or via uv's resolver when
the drift causes a cross-group conflict.}
}
\value{
Invisibly \code{NULL}. Side effect is one \code{message()} per
drifted dep.
}
\description{
Reads \verb{<pyproject_dir>/pyproject.toml} and, for groups in scope,
emits one message per dep whose pin differs from pyro's bundled
spec.
}
\details{
Groups in the project toml that aren't in the bundled spec (e.g.
third-party app groups, user-defined groups) are skipped silently.
Deps present in bundled but missing from the project are also skipped.

Cross-group pin conflicts (e.g. reportifyr pins \verb{pillow==10.0.0},
presentifyr pins \verb{pillow==11.1.0}) are not reported here either:
uv's lock pass spans the whole toml and surfaces "no solution found".

Called automatically by \code{\link[=initialize_python]{initialize_python()}} before sync, so drift
surfaces on every init regardless of how the wrapper structured its
setup.
}
\keyword{internal}
