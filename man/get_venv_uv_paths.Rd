% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/venv.R
\name{get_venv_uv_paths}
\alias{get_venv_uv_paths}
\title{Resolve paths to the uv binary and the project's \code{.venv} directory}
\usage{
get_venv_uv_paths()
}
\value{
Named list with \code{uv} (path to uv) and \code{venv} (path to the
\verb{.venv/} directory).
}
\description{
Looks up the project root via \code{\link[=get_proj_dir]{get_proj_dir()}} (which reads
\code{getOption("venv_dir")}, falling back to \code{\link[here:here]{here::here()}}) and caches
the resolved root in \code{options("venv_dir")} so subsequent calls are
cheap.
}
\examples{
\dontrun{
get_venv_uv_paths()
}
}
