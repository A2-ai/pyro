% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/initialize_python.R
\name{seed_pyproject}
\alias{seed_pyproject}
\title{Seed a project \code{pyproject.toml} from pyro's bundled spec}
\usage{
seed_pyproject(proj_dir, groups = NULL)
}
\arguments{
\item{proj_dir}{Project root directory. Must exist.}

\item{groups}{Character vector of group names to seed, or \code{NULL}
for all bundled groups.}
}
\description{
Writes a minimal \verb{[project]} table (\code{name = basename(proj_dir)},
\code{requires-python} from bundled) and a \verb{[dependency-groups]} table
containing the requested groups (or all bundled groups when
\code{groups = NULL}). Pins are copied verbatim from the bundled spec.
}
\details{
Caller is responsible for ensuring the toml does not already exist.
Errors if \code{proj_dir} does not exist, seeding never creates the
project directory itself.
}
\keyword{internal}
