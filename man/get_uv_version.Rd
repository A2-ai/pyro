% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/uv_path.R
\name{get_uv_version}
\alias{get_uv_version}
\title{Get the installed version of uv}
\usage{
get_uv_version(uv_path)
}
\arguments{
\item{uv_path}{Path to the uv executable. Typically obtained from
\code{\link[=get_uv_path]{get_uv_path()}}.}
}
\value{
Character scalar of the uv version (e.g., \code{"0.7.8"}).
}
\description{
Shells out to \code{uv --version} and parses the result. Useful for
diagnostics and for callers that need to gate behavior on the uv
version (e.g. only set a flag introduced in a specific release).
}
\examples{
\dontrun{
uv_path <- get_uv_path()
if (!is.null(uv_path)) get_uv_version(uv_path)
}
}
