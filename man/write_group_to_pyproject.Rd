% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/write_group_to_pyproject.R
\name{write_group_to_pyproject}
\alias{write_group_to_pyproject}
\title{Ensure a dependency group is present in the project's \code{pyproject.toml}}
\usage{
write_group_to_pyproject(name, deps = NULL, pyproject_dir = get_proj_dir())
}
\arguments{
\item{name}{Character group name (e.g. \code{"reportifyr"}).}

\item{deps}{Optional character vector of dependency strings
(e.g. \code{c("pillow==11.1.0", "requests==2.31.0")}). When \code{NULL}
(default), the bundled \code{pyproject.toml} is consulted for \code{name};
if \code{name} is not bundled, an error is raised.}

\item{pyproject_dir}{Directory containing the project's
\code{pyproject.toml}. Defaults to \code{\link[=get_proj_dir]{get_proj_dir()}}.}
}
\value{
Invisibly, \code{TRUE} if the toml was mutated, \code{FALSE} otherwise.
}
\description{
Idempotent merge helper called by sibling-package wrappers
(reportifyr, presentifyr) and third-party apps to wire their Python
dependency group into the project's \code{pyproject.toml}.
}
\details{
Merge semantics by case:
\describe{
\item{No \code{pyproject.toml}}{No-op, returns \code{FALSE}. Seeding is
\code{\link[=initialize_python]{initialize_python()}}'s job; this helper composes with it but
does not duplicate it.}
\item{No \verb{[dependency-groups]} table}{Append the table containing
\code{name}. Returns \code{TRUE}.}
\item{Table exists, no \code{name} subgroup}{Append the subgroup with
\code{deps}. Returns \code{TRUE}.}
\item{Subgroup exists, dep names match}{Per-dep version comparison.
If versions match: no-op, returns \code{FALSE}. If versions differ:
keep the user's pin (do not overwrite), emit a one-line message
per diff. Returns \code{FALSE}.}
\item{Subgroup exists, dep names differ}{Add any deps missing from
the project's group with the spec's pins. Never remove deps the
user added. Returns \code{TRUE}.}
}

The "spec" is pyro's bundled \code{pyproject.toml} for fyr-blessed
groups (when \code{deps = NULL}), or the caller-supplied \code{deps} for
third-party apps.
}
\examples{
\dontrun{
# fyr-blessed group: pins read from bundled spec
write_group_to_pyproject("reportifyr")

# third-party app: pins supplied explicitly
write_group_to_pyproject("myapp", deps = c("requests==2.31.0"))
}
}
